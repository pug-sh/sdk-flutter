// AUTOGENERATED — DO NOT EDIT.
//
// Regenerated by `make typed-track` (see tool/generate_track_namespace.dart).
// The hand-written scaffold (TrackContext, _merge, call,
// discouraged-path detector, _kindToDartMethodName) is preserved verbatim
// by the codegen template; only the per-event methods are derived from protos.

import 'package:meta/meta.dart';

import 'configuration.dart';
import 'contracts.dart';
import 'events.dart';
import 'well_known_events.dart';

/// The subset of `Pug` that `TrackNamespace` depends on. `Pug` implements
/// this. Defining it here (rather than importing `Pug`) breaks the circular
/// import between `pug.dart` and the codegen-target `track_namespace.dart`.
abstract interface class TrackContext {
  void capture(String kind, {Map<String, Object?> props, TrackOptions options});

  PugLogger get logger;
}

class TrackNamespace {
  const TrackNamespace(this._ctx);
  final TrackContext _ctx;

  /// Tracks a custom event by string kind. Matches the legacy `Pug.track`
  /// signature so that `Pug.track('kind', props: ...)` continues to work.
  void call(
    String kind, {
    Map<String, Object?> props = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    _warnDiscouragedPathOnce(kind);
    _ctx.capture(kind, props: props, options: options);
  }

  static final Set<String> _hintedKinds = <String>{};

  @visibleForTesting
  static void resetHintedKindsForTest() => _hintedKinds.clear();

  void _warnDiscouragedPathOnce(String kind) {
    if (!wellKnownEventSchemas.containsKey(kind)) return;
    if (!_hintedKinds.add(kind)) return;
    final dartName = _kindToDartMethodName(kind);
    _ctx.logger.debug(
      "Pug.track('$kind', ...) — consider Pug.track.$dartName(...) for "
      'compile-time typed properties.',
    );
  }

  static String _kindToDartMethodName(String snake) {
    final parts = snake.split('_').where((p) => p.isNotEmpty).toList();
    if (parts.isEmpty) return snake; // degenerate; pass through unchanged
    if (parts.length == 1) return parts.first;
    return parts.first +
        parts.skip(1).map((p) => p[0].toUpperCase() + p.substring(1)).join();
  }

  /// Tracks a `add_to_cart` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void addToCart({
    required String productId,
    double? price,
    String? currency,
    String? cartId,
    int? quantity,
    String? category,
    String? brand,
    String? sku,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.addToCart;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'addToCart',
        explicit: <String, Object?>{
          'productId': productId,
          if (price != null) 'price': price,
          if (currency != null) 'currency': currency,
          if (cartId != null) 'cartId': cartId,
          if (quantity != null) 'quantity': quantity,
          if (category != null) 'category': category,
          if (brand != null) 'brand': brand,
          if (sku != null) 'sku': sku,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `api_key_created` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void apiKeyCreated({
    String? apiKeyId,
    String? name,
    String? scope,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.apiKeyCreated;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'apiKeyCreated',
        explicit: <String, Object?>{
          if (apiKeyId != null) 'apiKeyId': apiKeyId,
          if (name != null) 'name': name,
          if (scope != null) 'scope': scope,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `api_key_revoked` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void apiKeyRevoked({
    String? apiKeyId,
    String? name,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.apiKeyRevoked;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'apiKeyRevoked',
        explicit: <String, Object?>{
          if (apiKeyId != null) 'apiKeyId': apiKeyId,
          if (name != null) 'name': name,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `app_backgrounded` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void appBackgrounded({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.appBackgrounded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'appBackgrounded',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `app_close` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void appClose({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.appClose;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'appClose',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `app_crashed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void appCrashed({
    String? errorMessage,
    String? errorType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.appCrashed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'appCrashed',
        explicit: <String, Object?>{
          if (errorMessage != null) 'errorMessage': errorMessage,
          if (errorType != null) 'errorType': errorType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `app_foregrounded` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void appForegrounded({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.appForegrounded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'appForegrounded',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `app_install` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void appInstall({
    String? appVersion,
    String? installSource,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.appInstall;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'appInstall',
        explicit: <String, Object?>{
          if (appVersion != null) 'appVersion': appVersion,
          if (installSource != null) 'installSource': installSource,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `app_open` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void appOpen({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.appOpen;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'appOpen',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `app_update` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void appUpdate({
    required String appVersion,
    String? previousVersion,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.appUpdate;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'appUpdate',
        explicit: <String, Object?>{
          'appVersion': appVersion,
          if (previousVersion != null) 'previousVersion': previousVersion,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `audio_completed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void audioCompleted({
    required String audioId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.audioCompleted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'audioCompleted',
        explicit: <String, Object?>{'audioId': audioId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `audio_pause` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void audioPause({
    required String audioId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.audioPause;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'audioPause',
        explicit: <String, Object?>{'audioId': audioId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `audio_play` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void audioPlay({
    required String audioId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.audioPlay;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'audioPlay',
        explicit: <String, Object?>{'audioId': audioId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `audio_seeked` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void audioSeeked({
    required String audioId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.audioSeeked;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'audioSeeked',
        explicit: <String, Object?>{'audioId': audioId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `audio_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void audioStarted({
    required String audioId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.audioStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'audioStarted',
        explicit: <String, Object?>{'audioId': audioId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `cart_viewed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void cartViewed({
    String? cartId,
    int? itemCount,
    double? amount,
    String? currency,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.cartViewed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'cartViewed',
        explicit: <String, Object?>{
          if (cartId != null) 'cartId': cartId,
          if (itemCount != null) 'itemCount': itemCount,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_archived` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatArchived({
    required String conversationId,
    String? conversationType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatArchived;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatArchived',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          if (conversationType != null) 'conversationType': conversationType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_attachment_downloaded` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatAttachmentDownloaded({
    required String conversationId,
    required String attachmentId,
    String? messageId,
    String? attachmentType,
    int? sizeBytes,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatAttachmentDownloaded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatAttachmentDownloaded',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'attachmentId': attachmentId,
          if (messageId != null) 'messageId': messageId,
          if (attachmentType != null) 'attachmentType': attachmentType,
          if (sizeBytes != null) 'sizeBytes': sizeBytes,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_attachment_uploaded` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatAttachmentUploaded({
    required String conversationId,
    required String attachmentId,
    String? messageId,
    String? attachmentType,
    int? sizeBytes,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatAttachmentUploaded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatAttachmentUploaded',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'attachmentId': attachmentId,
          if (messageId != null) 'messageId': messageId,
          if (attachmentType != null) 'attachmentType': attachmentType,
          if (sizeBytes != null) 'sizeBytes': sizeBytes,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_call_joined` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatCallJoined({
    required String conversationId,
    required String callId,
    String? callType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatCallJoined;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatCallJoined',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'callId': callId,
          if (callType != null) 'callType': callType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_call_left` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatCallLeft({
    required String conversationId,
    required String callId,
    String? callType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatCallLeft;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatCallLeft',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'callId': callId,
          if (callType != null) 'callType': callType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_call_recording_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatCallRecordingStarted({
    required String conversationId,
    required String callId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatCallRecordingStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatCallRecordingStarted',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'callId': callId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_call_screen_shared` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatCallScreenShared({
    required String conversationId,
    required String callId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatCallScreenShared;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatCallScreenShared',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'callId': callId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_call_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatCallStarted({
    required String conversationId,
    required String callId,
    String? callType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatCallStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatCallStarted',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'callId': callId,
          if (callType != null) 'callType': callType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_created` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatCreated({
    required String conversationId,
    String? conversationType,
    int? participantCount,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatCreated;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatCreated',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          if (conversationType != null) 'conversationType': conversationType,
          if (participantCount != null) 'participantCount': participantCount,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_deleted` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatDeleted({
    required String conversationId,
    String? conversationType,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatDeleted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatDeleted',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          if (conversationType != null) 'conversationType': conversationType,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_joined` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatJoined({
    required String conversationId,
    String? conversationType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatJoined;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatJoined',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          if (conversationType != null) 'conversationType': conversationType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_left` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatLeft({
    required String conversationId,
    String? conversationType,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatLeft;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatLeft',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          if (conversationType != null) 'conversationType': conversationType,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_member_added` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMemberAdded({
    required String conversationId,
    required String memberId,
    String? role,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMemberAdded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMemberAdded',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'memberId': memberId,
          if (role != null) 'role': role,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_member_muted` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMemberMuted({
    required String conversationId,
    required String memberId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMemberMuted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMemberMuted',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'memberId': memberId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_member_removed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMemberRemoved({
    required String conversationId,
    required String memberId,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMemberRemoved;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMemberRemoved',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'memberId': memberId,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_member_role_changed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMemberRoleChanged({
    required String conversationId,
    required String memberId,
    required String newRole,
    String? previousRole,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMemberRoleChanged;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMemberRoleChanged',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'memberId': memberId,
          'newRole': newRole,
          if (previousRole != null) 'previousRole': previousRole,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_message_deleted` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMessageDeleted({
    required String conversationId,
    required String messageId,
    String? conversationType,
    String? reason,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMessageDeleted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMessageDeleted',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'messageId': messageId,
          if (conversationType != null) 'conversationType': conversationType,
          if (reason != null) 'reason': reason,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_message_edited` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMessageEdited({
    required String conversationId,
    required String messageId,
    String? conversationType,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMessageEdited;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMessageEdited',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'messageId': messageId,
          if (conversationType != null) 'conversationType': conversationType,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_message_failed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMessageFailed({
    required String conversationId,
    required String reason,
    String? messageId,
    String? conversationType,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMessageFailed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMessageFailed',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'reason': reason,
          if (messageId != null) 'messageId': messageId,
          if (conversationType != null) 'conversationType': conversationType,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_message_pinned` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMessagePinned({
    required String conversationId,
    required String messageId,
    String? conversationType,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMessagePinned;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMessagePinned',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'messageId': messageId,
          if (conversationType != null) 'conversationType': conversationType,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_message_read` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMessageRead({
    required String conversationId,
    required String messageId,
    String? conversationType,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMessageRead;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMessageRead',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'messageId': messageId,
          if (conversationType != null) 'conversationType': conversationType,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_message_received` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMessageReceived({
    required String conversationId,
    required String messageId,
    String? conversationType,
    String? messageType,
    int? characterCount,
    int? attachmentCount,
    String? threadId,
    String? parentMessageId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMessageReceived;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMessageReceived',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'messageId': messageId,
          if (conversationType != null) 'conversationType': conversationType,
          if (messageType != null) 'messageType': messageType,
          if (characterCount != null) 'characterCount': characterCount,
          if (attachmentCount != null) 'attachmentCount': attachmentCount,
          if (threadId != null) 'threadId': threadId,
          if (parentMessageId != null) 'parentMessageId': parentMessageId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_message_sent` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMessageSent({
    required String conversationId,
    required String messageId,
    String? conversationType,
    String? messageType,
    int? characterCount,
    int? attachmentCount,
    String? threadId,
    String? parentMessageId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMessageSent;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMessageSent',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'messageId': messageId,
          if (conversationType != null) 'conversationType': conversationType,
          if (messageType != null) 'messageType': messageType,
          if (characterCount != null) 'characterCount': characterCount,
          if (attachmentCount != null) 'attachmentCount': attachmentCount,
          if (threadId != null) 'threadId': threadId,
          if (parentMessageId != null) 'parentMessageId': parentMessageId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_message_unpinned` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatMessageUnpinned({
    required String conversationId,
    required String messageId,
    String? conversationType,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatMessageUnpinned;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatMessageUnpinned',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'messageId': messageId,
          if (conversationType != null) 'conversationType': conversationType,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_reaction_added` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatReactionAdded({
    required String conversationId,
    required String messageId,
    required String reaction,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatReactionAdded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatReactionAdded',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'messageId': messageId,
          'reaction': reaction,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_reaction_removed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatReactionRemoved({
    required String conversationId,
    required String messageId,
    required String reaction,
    String? threadId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatReactionRemoved;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatReactionRemoved',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          'messageId': messageId,
          'reaction': reaction,
          if (threadId != null) 'threadId': threadId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_typing_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatTypingStarted({
    required String conversationId,
    String? conversationType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatTypingStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatTypingStarted',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          if (conversationType != null) 'conversationType': conversationType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_typing_stopped` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatTypingStopped({
    required String conversationId,
    String? conversationType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatTypingStopped;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatTypingStopped',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          if (conversationType != null) 'conversationType': conversationType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_unarchived` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatUnarchived({
    required String conversationId,
    String? conversationType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatUnarchived;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatUnarchived',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          if (conversationType != null) 'conversationType': conversationType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `chat_user_blocked` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void chatUserBlocked({
    required String userId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.chatUserBlocked;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'chatUserBlocked',
        explicit: <String, Object?>{'userId': userId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `checkout_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void checkoutStarted({
    String? productId,
    double? amount,
    String? currency,
    String? cartId,
    String? checkoutId,
    int? itemCount,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.checkoutStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'checkoutStarted',
        explicit: <String, Object?>{
          if (productId != null) 'productId': productId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
          if (cartId != null) 'cartId': cartId,
          if (checkoutId != null) 'checkoutId': checkoutId,
          if (itemCount != null) 'itemCount': itemCount,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `checkout_step_completed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void checkoutStepCompleted({
    required String checkoutId,
    required String step,
    int? stepIndex,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.checkoutStepCompleted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'checkoutStepCompleted',
        explicit: <String, Object?>{
          'checkoutId': checkoutId,
          'step': step,
          if (stepIndex != null) 'stepIndex': stepIndex,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `coupon_applied` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void couponApplied({
    required String couponId,
    String? couponCode,
    String? cartId,
    double? discountAmount,
    String? currency,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.couponApplied;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'couponApplied',
        explicit: <String, Object?>{
          'couponId': couponId,
          if (couponCode != null) 'couponCode': couponCode,
          if (cartId != null) 'cartId': cartId,
          if (discountAmount != null) 'discountAmount': discountAmount,
          if (currency != null) 'currency': currency,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `coupon_removed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void couponRemoved({
    required String couponId,
    String? couponCode,
    String? cartId,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.couponRemoved;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'couponRemoved',
        explicit: <String, Object?>{
          'couponId': couponId,
          if (couponCode != null) 'couponCode': couponCode,
          if (cartId != null) 'cartId': cartId,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `email_verified` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void emailVerified({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.emailVerified;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'emailVerified',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `error_occurred` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void errorOccurred({
    required String errorCode,
    String? message,
    String? severity,
    bool? unhandled,
    String? stack,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.errorOccurred;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'errorOccurred',
        explicit: <String, Object?>{
          'errorCode': errorCode,
          if (message != null) 'message': message,
          if (severity != null) 'severity': severity,
          if (unhandled != null) 'unhandled': unhandled,
          if (stack != null) 'stack': stack,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `export_completed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void exportCompleted({
    required String exportId,
    String? exportType,
    int? sizeBytes,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.exportCompleted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'exportCompleted',
        explicit: <String, Object?>{
          'exportId': exportId,
          if (exportType != null) 'exportType': exportType,
          if (sizeBytes != null) 'sizeBytes': sizeBytes,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `export_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void exportStarted({
    required String exportId,
    String? exportType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.exportStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'exportStarted',
        explicit: <String, Object?>{
          'exportId': exportId,
          if (exportType != null) 'exportType': exportType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `feature_used` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void featureUsed({
    required String featureId,
    String? featureName,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.featureUsed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'featureUsed',
        explicit: <String, Object?>{
          'featureId': featureId,
          if (featureName != null) 'featureName': featureName,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `feedback_submitted` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void feedbackSubmitted({
    String? feedbackId,
    String? category,
    String? comment,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.feedbackSubmitted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'feedbackSubmitted',
        explicit: <String, Object?>{
          if (feedbackId != null) 'feedbackId': feedbackId,
          if (category != null) 'category': category,
          if (comment != null) 'comment': comment,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `file_downloaded` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void fileDownloaded({
    required String fileId,
    String? fileName,
    String? fileType,
    int? sizeBytes,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.fileDownloaded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'fileDownloaded',
        explicit: <String, Object?>{
          'fileId': fileId,
          if (fileName != null) 'fileName': fileName,
          if (fileType != null) 'fileType': fileType,
          if (sizeBytes != null) 'sizeBytes': sizeBytes,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `file_uploaded` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void fileUploaded({
    required String fileId,
    String? fileName,
    String? fileType,
    int? sizeBytes,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.fileUploaded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'fileUploaded',
        explicit: <String, Object?>{
          'fileId': fileId,
          if (fileName != null) 'fileName': fileName,
          if (fileType != null) 'fileType': fileType,
          if (sizeBytes != null) 'sizeBytes': sizeBytes,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `filter_applied` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void filterApplied({
    required String key,
    String? value,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.filterApplied;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'filterApplied',
        explicit: <String, Object?>{
          'key': key,
          if (value != null) 'value': value,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `form_start` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void formStart({
    required String formId,
    String? formName,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.formStart;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'formStart',
        explicit: <String, Object?>{
          'formId': formId,
          if (formName != null) 'formName': formName,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `form_submit` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void formSubmit({
    required String formId,
    String? formName,
    String? action,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.formSubmit;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'formSubmit',
        explicit: <String, Object?>{
          'formId': formId,
          if (formName != null) 'formName': formName,
          if (action != null) 'action': action,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `help_article_viewed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void helpArticleViewed({
    required String articleId,
    String? articleTitle,
    String? category,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.helpArticleViewed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'helpArticleViewed',
        explicit: <String, Object?>{
          'articleId': articleId,
          if (articleTitle != null) 'articleTitle': articleTitle,
          if (category != null) 'category': category,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `integration_connected` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void integrationConnected({
    required String integrationType,
    String? integrationId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.integrationConnected;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'integrationConnected',
        explicit: <String, Object?>{
          'integrationType': integrationType,
          if (integrationId != null) 'integrationId': integrationId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `integration_disconnected` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void integrationDisconnected({
    required String integrationType,
    String? integrationId,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.integrationDisconnected;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'integrationDisconnected',
        explicit: <String, Object?>{
          'integrationType': integrationType,
          if (integrationId != null) 'integrationId': integrationId,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `invite_accepted` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void inviteAccepted({
    String? inviteId,
    String? workspaceId,
    String? inviterId,
    String? inviteeId,
    String? inviteeEmail,
    String? role,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.inviteAccepted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'inviteAccepted',
        explicit: <String, Object?>{
          if (inviteId != null) 'inviteId': inviteId,
          if (workspaceId != null) 'workspaceId': workspaceId,
          if (inviterId != null) 'inviterId': inviterId,
          if (inviteeId != null) 'inviteeId': inviteeId,
          if (inviteeEmail != null) 'inviteeEmail': inviteeEmail,
          if (role != null) 'role': role,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `invite_sent` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void inviteSent({
    String? inviteId,
    String? workspaceId,
    String? inviterId,
    String? inviteeId,
    String? inviteeEmail,
    String? role,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.inviteSent;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'inviteSent',
        explicit: <String, Object?>{
          if (inviteId != null) 'inviteId': inviteId,
          if (workspaceId != null) 'workspaceId': workspaceId,
          if (inviterId != null) 'inviterId': inviterId,
          if (inviteeId != null) 'inviteeId': inviteeId,
          if (inviteeEmail != null) 'inviteeEmail': inviteeEmail,
          if (role != null) 'role': role,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `invoice_failed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void invoiceFailed({
    required String invoiceId,
    String? subscriptionId,
    double? amount,
    String? currency,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.invoiceFailed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'invoiceFailed',
        explicit: <String, Object?>{
          'invoiceId': invoiceId,
          if (subscriptionId != null) 'subscriptionId': subscriptionId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `invoice_paid` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void invoicePaid({
    required String invoiceId,
    String? subscriptionId,
    double? amount,
    String? currency,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.invoicePaid;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'invoicePaid',
        explicit: <String, Object?>{
          'invoiceId': invoiceId,
          if (subscriptionId != null) 'subscriptionId': subscriptionId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `mfa_disabled` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void mfaDisabled({
    String? method,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.mfaDisabled;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'mfaDisabled',
        explicit: <String, Object?>{if (method != null) 'method': method},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `mfa_enabled` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void mfaEnabled({
    String? method,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.mfaEnabled;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'mfaEnabled',
        explicit: <String, Object?>{if (method != null) 'method': method},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `notification_clicked` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void notificationClicked({
    required String campaignId,
    String? notificationType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.notificationClicked;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'notificationClicked',
        explicit: <String, Object?>{
          'campaignId': campaignId,
          if (notificationType != null) 'notificationType': notificationType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `notification_dismissed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void notificationDismissed({
    required String campaignId,
    String? notificationType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.notificationDismissed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'notificationDismissed',
        explicit: <String, Object?>{
          'campaignId': campaignId,
          if (notificationType != null) 'notificationType': notificationType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `notification_received` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void notificationReceived({
    required String campaignId,
    String? notificationType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.notificationReceived;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'notificationReceived',
        explicit: <String, Object?>{
          'campaignId': campaignId,
          if (notificationType != null) 'notificationType': notificationType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `nps_submitted` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void npsSubmitted({
    int? score,
    String? comment,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.npsSubmitted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'npsSubmitted',
        explicit: <String, Object?>{
          if (score != null) 'score': score,
          if (comment != null) 'comment': comment,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `order_refunded` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void orderRefunded({
    required String orderId,
    double? amount,
    String? currency,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.orderRefunded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'orderRefunded',
        explicit: <String, Object?>{
          'orderId': orderId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `password_reset_completed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void passwordResetCompleted({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.passwordResetCompleted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'passwordResetCompleted',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `password_reset_requested` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void passwordResetRequested({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.passwordResetRequested;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'passwordResetRequested',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `payment_failed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void paymentFailed({
    String? paymentId,
    String? invoiceId,
    String? subscriptionId,
    double? amount,
    String? currency,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.paymentFailed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'paymentFailed',
        explicit: <String, Object?>{
          if (paymentId != null) 'paymentId': paymentId,
          if (invoiceId != null) 'invoiceId': invoiceId,
          if (subscriptionId != null) 'subscriptionId': subscriptionId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `payment_method_added` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void paymentMethodAdded({
    String? paymentMethodId,
    String? paymentMethodType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.paymentMethodAdded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'paymentMethodAdded',
        explicit: <String, Object?>{
          if (paymentMethodId != null) 'paymentMethodId': paymentMethodId,
          if (paymentMethodType != null) 'paymentMethodType': paymentMethodType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `payment_method_removed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void paymentMethodRemoved({
    String? paymentMethodId,
    String? paymentMethodType,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.paymentMethodRemoved;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'paymentMethodRemoved',
        explicit: <String, Object?>{
          if (paymentMethodId != null) 'paymentMethodId': paymentMethodId,
          if (paymentMethodType != null) 'paymentMethodType': paymentMethodType,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `payment_succeeded` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void paymentSucceeded({
    String? paymentId,
    String? invoiceId,
    String? subscriptionId,
    double? amount,
    String? currency,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.paymentSucceeded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'paymentSucceeded',
        explicit: <String, Object?>{
          if (paymentId != null) 'paymentId': paymentId,
          if (invoiceId != null) 'invoiceId': invoiceId,
          if (subscriptionId != null) 'subscriptionId': subscriptionId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `product_list_viewed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void productListViewed({
    required String listId,
    String? listName,
    String? category,
    int? itemCount,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.productListViewed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'productListViewed',
        explicit: <String, Object?>{
          'listId': listId,
          if (listName != null) 'listName': listName,
          if (category != null) 'category': category,
          if (itemCount != null) 'itemCount': itemCount,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `product_viewed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void productViewed({
    required String productId,
    String? productName,
    String? category,
    String? brand,
    String? sku,
    double? price,
    String? currency,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.productViewed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'productViewed',
        explicit: <String, Object?>{
          'productId': productId,
          if (productName != null) 'productName': productName,
          if (category != null) 'category': category,
          if (brand != null) 'brand': brand,
          if (sku != null) 'sku': sku,
          if (price != null) 'price': price,
          if (currency != null) 'currency': currency,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `purchase` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void purchase({
    String? productId,
    double? amount,
    String? currency,
    String? orderId,
    int? quantity,
    String? category,
    String? brand,
    String? sku,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.purchase;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'purchase',
        explicit: <String, Object?>{
          if (productId != null) 'productId': productId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
          if (orderId != null) 'orderId': orderId,
          if (quantity != null) 'quantity': quantity,
          if (category != null) 'category': category,
          if (brand != null) 'brand': brand,
          if (sku != null) 'sku': sku,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `recommendation_clicked` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void recommendationClicked({
    required String recommendationId,
    required String itemId,
    String? source,
    int? index,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.recommendationClicked;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'recommendationClicked',
        explicit: <String, Object?>{
          'recommendationId': recommendationId,
          'itemId': itemId,
          if (source != null) 'source': source,
          if (index != null) 'index': index,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `recommendation_viewed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void recommendationViewed({
    required String recommendationId,
    String? itemId,
    String? source,
    int? index,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.recommendationViewed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'recommendationViewed',
        explicit: <String, Object?>{
          'recommendationId': recommendationId,
          if (itemId != null) 'itemId': itemId,
          if (source != null) 'source': source,
          if (index != null) 'index': index,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `refund_failed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void refundFailed({
    required String orderId,
    String? refundId,
    double? amount,
    String? currency,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.refundFailed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'refundFailed',
        explicit: <String, Object?>{
          'orderId': orderId,
          if (refundId != null) 'refundId': refundId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `remove_from_cart` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void removeFromCart({
    required String productId,
    double? price,
    String? currency,
    String? cartId,
    int? quantity,
    String? category,
    String? brand,
    String? sku,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.removeFromCart;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'removeFromCart',
        explicit: <String, Object?>{
          'productId': productId,
          if (price != null) 'price': price,
          if (currency != null) 'currency': currency,
          if (cartId != null) 'cartId': cartId,
          if (quantity != null) 'quantity': quantity,
          if (category != null) 'category': category,
          if (brand != null) 'brand': brand,
          if (sku != null) 'sku': sku,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `screen_view` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void screenView({
    required String screenName,
    String? screenClass,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.screenView;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'screenView',
        explicit: <String, Object?>{
          'screenName': screenName,
          if (screenClass != null) 'screenClass': screenClass,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `scroll` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void scroll({
    int? percent,
    int? scrollY,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.scroll;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'scroll',
        explicit: <String, Object?>{
          if (percent != null) 'percent': percent,
          if (scrollY != null) 'scrollY': scrollY,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `search` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void search({
    required String query,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.search;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'search',
        explicit: <String, Object?>{'query': query},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `search_result_clicked` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void searchResultClicked({
    required String query,
    required String resultId,
    int? index,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.searchResultClicked;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'searchResultClicked',
        explicit: <String, Object?>{
          'query': query,
          'resultId': resultId,
          if (index != null) 'index': index,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `share` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void share({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.share;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'share',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `signin` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void signin({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.signin;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'signin',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `signout` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void signout({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.signout;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'signout',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `signup` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void signup({
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.signup;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'signup',
        explicit: <String, Object?>{},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `sort_changed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void sortChanged({
    required String key,
    String? direction,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.sortChanged;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'sortChanged',
        explicit: <String, Object?>{
          'key': key,
          if (direction != null) 'direction': direction,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `subscription_canceled` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void subscriptionCanceled({
    required String subscriptionId,
    String? planId,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.subscriptionCanceled;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'subscriptionCanceled',
        explicit: <String, Object?>{
          'subscriptionId': subscriptionId,
          if (planId != null) 'planId': planId,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `subscription_changed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void subscriptionChanged({
    required String subscriptionId,
    String? previousPlanId,
    String? newPlanId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.subscriptionChanged;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'subscriptionChanged',
        explicit: <String, Object?>{
          'subscriptionId': subscriptionId,
          if (previousPlanId != null) 'previousPlanId': previousPlanId,
          if (newPlanId != null) 'newPlanId': newPlanId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `subscription_paused` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void subscriptionPaused({
    required String subscriptionId,
    String? planId,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.subscriptionPaused;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'subscriptionPaused',
        explicit: <String, Object?>{
          'subscriptionId': subscriptionId,
          if (planId != null) 'planId': planId,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `subscription_renewed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void subscriptionRenewed({
    required String subscriptionId,
    String? planId,
    double? amount,
    String? currency,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.subscriptionRenewed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'subscriptionRenewed',
        explicit: <String, Object?>{
          'subscriptionId': subscriptionId,
          if (planId != null) 'planId': planId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `subscription_resumed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void subscriptionResumed({
    required String subscriptionId,
    String? planId,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.subscriptionResumed;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'subscriptionResumed',
        explicit: <String, Object?>{
          'subscriptionId': subscriptionId,
          if (planId != null) 'planId': planId,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `subscription_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void subscriptionStarted({
    required String subscriptionId,
    String? planId,
    double? amount,
    String? currency,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.subscriptionStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'subscriptionStarted',
        explicit: <String, Object?>{
          'subscriptionId': subscriptionId,
          if (planId != null) 'planId': planId,
          if (amount != null) 'amount': amount,
          if (currency != null) 'currency': currency,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `subscription_trial_will_end` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void subscriptionTrialWillEnd({
    required String subscriptionId,
    String? planId,
    String? trialId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.subscriptionTrialWillEnd;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'subscriptionTrialWillEnd',
        explicit: <String, Object?>{
          'subscriptionId': subscriptionId,
          if (planId != null) 'planId': planId,
          if (trialId != null) 'trialId': trialId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `support_chat_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void supportChatStarted({
    required String conversationId,
    String? topic,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.supportChatStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'supportChatStarted',
        explicit: <String, Object?>{
          'conversationId': conversationId,
          if (topic != null) 'topic': topic,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `support_ticket_created` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void supportTicketCreated({
    required String ticketId,
    String? category,
    String? priority,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.supportTicketCreated;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'supportTicketCreated',
        explicit: <String, Object?>{
          'ticketId': ticketId,
          if (category != null) 'category': category,
          if (priority != null) 'priority': priority,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `support_ticket_resolved` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void supportTicketResolved({
    required String ticketId,
    String? resolution,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.supportTicketResolved;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'supportTicketResolved',
        explicit: <String, Object?>{
          'ticketId': ticketId,
          if (resolution != null) 'resolution': resolution,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `survey_completed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void surveyCompleted({
    required String surveyId,
    int? questionCount,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.surveyCompleted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'surveyCompleted',
        explicit: <String, Object?>{
          'surveyId': surveyId,
          if (questionCount != null) 'questionCount': questionCount,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `survey_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void surveyStarted({
    required String surveyId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.surveyStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'surveyStarted',
        explicit: <String, Object?>{'surveyId': surveyId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `trial_converted` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void trialConverted({
    String? trialId,
    String? subscriptionId,
    String? planId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.trialConverted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'trialConverted',
        explicit: <String, Object?>{
          if (trialId != null) 'trialId': trialId,
          if (subscriptionId != null) 'subscriptionId': subscriptionId,
          if (planId != null) 'planId': planId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `trial_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void trialStarted({
    String? trialId,
    String? planId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.trialStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'trialStarted',
        explicit: <String, Object?>{
          if (trialId != null) 'trialId': trialId,
          if (planId != null) 'planId': planId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `video_completed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void videoCompleted({
    required String videoId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.videoCompleted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'videoCompleted',
        explicit: <String, Object?>{'videoId': videoId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `video_pause` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void videoPause({
    required String videoId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.videoPause;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'videoPause',
        explicit: <String, Object?>{'videoId': videoId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `video_play` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void videoPlay({
    required String videoId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.videoPlay;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'videoPlay',
        explicit: <String, Object?>{'videoId': videoId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `video_seeked` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void videoSeeked({
    required String videoId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.videoSeeked;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'videoSeeked',
        explicit: <String, Object?>{'videoId': videoId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `video_started` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void videoStarted({
    required String videoId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.videoStarted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'videoStarted',
        explicit: <String, Object?>{'videoId': videoId},
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `wishlist_added` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void wishlistAdded({
    required String productId,
    String? wishlistId,
    double? price,
    String? currency,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.wishlistAdded;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'wishlistAdded',
        explicit: <String, Object?>{
          'productId': productId,
          if (wishlistId != null) 'wishlistId': wishlistId,
          if (price != null) 'price': price,
          if (currency != null) 'currency': currency,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `wishlist_removed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void wishlistRemoved({
    required String productId,
    String? wishlistId,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.wishlistRemoved;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'wishlistRemoved',
        explicit: <String, Object?>{
          'productId': productId,
          if (wishlistId != null) 'wishlistId': wishlistId,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `workspace_created` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void workspaceCreated({
    required String workspaceId,
    String? workspaceName,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.workspaceCreated;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'workspaceCreated',
        explicit: <String, Object?>{
          'workspaceId': workspaceId,
          if (workspaceName != null) 'workspaceName': workspaceName,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `workspace_deleted` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void workspaceDeleted({
    required String workspaceId,
    String? reason,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.workspaceDeleted;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'workspaceDeleted',
        explicit: <String, Object?>{
          'workspaceId': workspaceId,
          if (reason != null) 'reason': reason,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `workspace_joined` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void workspaceJoined({
    required String workspaceId,
    String? role,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.workspaceJoined;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'workspaceJoined',
        explicit: <String, Object?>{
          'workspaceId': workspaceId,
          if (role != null) 'role': role,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `workspace_role_changed` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void workspaceRoleChanged({
    required String workspaceId,
    required String memberId,
    required String newRole,
    String? previousRole,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.workspaceRoleChanged;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'workspaceRoleChanged',
        explicit: <String, Object?>{
          'workspaceId': workspaceId,
          'memberId': memberId,
          'newRole': newRole,
          if (previousRole != null) 'previousRole': previousRole,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Tracks a `workspace_settings_updated` event.
  ///
  /// Named args define the typed schema fields and always win over [extras].
  /// Any [extras] key that collides with a named arg is dropped and logged
  /// at WARN level (the named arg always wins).
  void workspaceSettingsUpdated({
    required String workspaceId,
    required String setting,
    Map<String, Object?> extras = const {},
    TrackOptions options = const TrackOptions(),
  }) {
    const eventName = PugEventNames.workspaceSettingsUpdated;
    _ctx.capture(
      eventName,
      props: _merge(
        methodName: 'workspaceSettingsUpdated',
        explicit: <String, Object?>{
          'workspaceId': workspaceId,
          'setting': setting,
        },
        extras: extras,
      ),
      options: options,
    );
  }

  /// Merges [extras] behind [explicit], dropping any extras key that collides
  /// with an explicit named arg and logging a WARN message for each dropped key.
  ///
  /// [methodName] is the camelCase Dart method name (e.g. `addToCart`) — not
  /// the wire-format kind — so log messages match the call site users wrote.
  Map<String, Object?> _merge({
    required String methodName,
    required Map<String, Object?> explicit,
    required Map<String, Object?> extras,
  }) {
    if (extras.isEmpty) return explicit;
    final result = <String, Object?>{};
    for (final entry in extras.entries) {
      if (explicit.containsKey(entry.key)) {
        _ctx.logger.warn(
          'Pug.track.$methodName: extras key "${entry.key}" overridden by explicit named arg.',
        );
        continue;
      }
      result[entry.key] = entry.value;
    }
    result.addAll(explicit);
    return result;
  }
}
